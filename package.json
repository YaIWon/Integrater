{
  "name": "universal-integrator-pro",
  "version": "4.0.0",
  "description": "Universal File Integration System",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Building...' && mkdir -p dist && cp index.html dist/",
    "deploy:pages": "npm run build && npx gh-pages -d dist --no-history --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.0.0"
  }
}
